[package]
name = "app-run"
description = "Run wrapper for Nix AppImage"
version = "0.2.0"
edition = "2021"

[dependencies]
clap = { version = "4", features = ["derive"] }
env_logger = "0.11"
log = "0.4"
nix = { version = "0.29", features = ["fs", "mount", "sched", "process", "user"] }
paste = "1"
